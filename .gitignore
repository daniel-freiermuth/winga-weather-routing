dist/
node_modules/
public/buildinfo.json
*.tgz
obsolete because claude did wrong/
.claude/

# GSHHG build artifacts — keep only the final compressed indices
data/gshhg-*.zip
data/_tmp_shp/
data/*.bin
!data/*.bin.gz

# Sub-package binary artifacts — built in CI, not committed
packages/*/binding/
